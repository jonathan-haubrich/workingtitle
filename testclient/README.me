#### The client handles communications between local and remote

It presents a command loop to the user that shows all the available modules. A user can select a module to access the commands within the module.

